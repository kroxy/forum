#pages
  .panel.panel-default
    .panel-header
      %h2=@post.title
    .panel-body
      %p=@post.content
      #comments
        %h2=@post.comments.count
        %p
        hozzászolás
        =render @post.comments
        %h2 Szoljon hozzá Ön is a témához!
        =render 'comments/form'
